import { Router } from '@angular/router';
import React, { FC } from 'react';

interface ImcBannerModuleProps {
  routerData?: string;
}

export const ImcBannerModule: FC<ImcBannerModuleProps> = ({ routerData }) => {
  return (
    <div>
      {/* testrttttttttt
      {JSON.stringify(routerData)} */}
    </div>
  )
};
